#include <stdio.h>
#include <math.h>

int	main(){
	int N, arr[500001] = {0};
	scanf("%d, &N);
	if(N%2 == 0 && 2){
		for(int idx = 0; idx <  ; idx++){
			if(pow(2, idx) <= N && (powO2, idx+1) > N){
				printf("%d", pow(2, idx));
				return 0;
			}
		}
	else




	return 0;
}
